<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="product.mapper">

<select id="genreKo" parameterType="com.webook.domain.ProductVO" resultType="com.webook.domain.ProductVO">

SELECT *
FROM product_list
<where>

<choose>
<when test="genre_no != null" >
 genre_no = #{genre_no}
</when>
<otherwise>
PRODUCT_NAME like '%'||'${searchKeyword}'||'%' or PRODUCT_WRITER like '%'||'${searchKeyword}'||'%'
</otherwise>
</choose>
</where>

</select>

<select id="genreFo" parameterType="com.webook.domain.ProductVO" resultType="com.webook.domain.ProductVO">

SELECT *
FROM product_list
<where>
 genre_no = #{genre_no}
</where>

</select>

<select id="getProduct" parameterType="com.webook.domain.ProductVO" resultType="com.webook.domain.ProductVO">

SELECT *
FROM product_list
WHERE product_no=#{product_no}


</select>

<select id="addCart" parameterType="com.webook.domain.ProductVO" resultType="com.webook.domain.ProductVO">

select product_no, product_name, product_price, product_image, product_cnt
FROM product_list

<if test="#{product_cnt} != null">
<where>
product_no=#{product_no} 
</where> 
</if>
</select>



</mapper>